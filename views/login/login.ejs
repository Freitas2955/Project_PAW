<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel='stylesheet' href='/stylesheets/login/login.css' />
</head>

<body>
    <nav>
        <div id="imagemlogo">
            <img src="../images/logo.png" alt="Logo" width="130" height="130"/>
        </div>
        <div id="blococentral">
            <h2>Iniciar sessão</h2>
            <form action="/login/loginSubmitted" method="POST">
                <div class="grupo">
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" placeholder="Email" required>
                </div>
                <div class="grupo">
                    <label for="password">Password:</label>
                    <div class="password-input">
                        <input type="password" id="password" name="password" placeholder="Password" required>
                        <span class="mudar" onclick="togglePasswordVisibility()">&#x1F441;</span>
                    </div>
                </div>
                <div class="grupo">
                    <input type="checkbox" id="rememberMe" name="rememberMe">
                    <label for="rememberMe">Lembrar-me</label>
                </div>
                <button type="submit" id="botao">Entrar</button>
            </form>
            <div class="recuperarpass">
                <a href="recuperar-password.html">Recuperar senha?</a>
            </div>
        </div>
    </nav>
</body>
</html>

<script>
    function togglePasswordVisibility() {
    const passwordInput = document.getElementById('password');
    const icon = document.querySelector('.mudar');

    if (passwordInput.type === 'password') {
        passwordInput.type = 'text';
        icon.textContent = '👁️';
    } else {
        passwordInput.type = 'password';
        icon.textContent = '👁️';
    }
}
</script>